import React from 'react';

// This component is not used because authentication has been disabled.
// It is neutralized to prevent accidental use.
const AuthModal = () => null;

export default AuthModal;
